{
  "name": "gdsbeds-social-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic internal preview renderer for GDS BEDS social media automation.",
  "scripts": {
    "validate": "node src/validate.mjs",
    "render": "node src/render.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
